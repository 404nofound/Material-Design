<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eddy.materialdesign">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/main_icon"
        android:roundIcon="@mipmap/main_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.fullscreen.FullScreenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:theme="@style/FullscreenTheme"/>

        <activity android:name=".ui.bottomnavigation.BottomNavigationActivity" />

        <activity android:name=".ui.bottomappbar.BottomAppBarActivity" />

        <activity android:name=".ui.recyclerview.RecyclerViewActivity" />

        <activity
            android:name=".ui.recyclerview.ItemActivity"
            android:theme="@style/FruitActivityTheme" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-4075829839329155~5161317796"/>
    </application>

</manifest>